__pycache__
.DS_Store
.venv

*.wav

# CoreML models (large binaries - download from HuggingFace)
*.mlpackage
*.mlmodelc

# Large numpy arrays (exported constants - regenerate via export_constants.py)
*.npy

# PyTorch model weights (download from HuggingFace)
*.safetensors
*.bin
*.pt
*.pth
*.ckpt
*.nemo

# ONNX models
*.onnx

# PocketTTS multi-language build output (~33 GB; staged for HF upload only)
models/tts/pocket_tts/coreml/v2/
models/tts/pocket_tts/coreml/build
models/tts/pocket_tts/coreml/build/

# PocketTTS upstream source pulled by uv (not part of this repo)
models/tts/pocket_tts/pocket_tts/

# Swift package build artifacts
.build/
Package.resolved

# Benchmark run logs (noisy HF progress bars + stack traces); JSON results are kept
models/stt/nemotron-asr-streaming-multilingual-0.6b/coreml/bench_results/*.log
