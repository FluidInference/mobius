# Kai conversion status

- Native checkpoint: pinned in `assets.lock.json`; all four weight files and complete 571,909,635-parameter runtime present.
- Provenance: native manifest, model code, config, and all four weight hashes match the last Hub commit before the tracker snapshot. The tracker serving adapter and exact evaluated checkpoint remain unverified.
- CPU native parity: Choice, Noul, and Score matched their typed exporter wrappers exactly on real source examples.
- Core ML: all three FP16 paths exported at L128. Choice uses K3, Noul K2, and Score K3. Native-to-wrapper and trace errors were 0. Across the source repository's six real examples, all top decisions match. Maximum probability errors were Choice 0.00115, Noul 0.00322, and Score 0.000485. Each package is 643,804,060 bytes; installed total is 1,931,412,180 bytes because the independent graphs duplicate the shared embedding weights. See `reports/` for the exact per-request evidence. The public 1,024-token and variable-candidate contract is not yet covered.
- Host runtime: a complete three-question System One request succeeded against the staged Core ML package, returning Choice, Noul, and Score answers. Core ML aborts when the second typed package is loaded while the first is retained; the host releases the previous package before loading the next. This keeps only one ~644 MB package resident at a time, at the cost of a cold load when question types alternate.
- Hugging Face: [FluidInference/decision-1.0-kai-coreml](https://huggingface.co/FluidInference/decision-1.0-kai-coreml), revision `56ee0e90f33f57771b411d377135ce07acb5de1f`, contains all three FP16 paths, three optional W8 paths, tokenizer, code, licensing, and reports. It is labeled as a fixed-L128 preview, not full 1,024-token support.
- Optional embedding-only W8 Choice/Noul/Score packages each save 195.8 MB and pass 2/2 pinned native decisions per path under the 0.02 probability gate. The Choice compute plan assigned 926/938 executable ops to ANE but did not establish a repeatable latency gain. See `reports/embedding-w8.json`.
